import { useMemo } from "react";
function formatDate(value){if(!value)return "—";const d=new Date(`${String(value).slice(0,10)}T00:00:00`);return Number.isNaN(d.getTime())?String(value):d.toLocaleDateString("pt-BR");}
function BarChart({data}){const max=Math.max(1,...data.map(x=>x.value));return <div className="vacation-chart" aria-label="Distribuição dos períodos de férias"><div className="chart-bars">{data.map(item=><div className="chart-col" key={item.label}><div className="chart-bar-wrap"><div className="chart-bar" style={{height:`${Math.max(6,item.value/max*100)}%`}} title={`${item.label}: ${item.value}`}>{item.value>0&&<span>{item.value}</span>}</div></div><small>{item.label}</small></div>)}</div></div>}
export default function VacationDashboard({role="employee",rows=[]}){
 const admin=["rh","dp","hr","admin","administrator","master"].includes(String(role).toLowerCase());
 const metrics=useMemo(()=>({pending:rows.filter(r=>["pendente","solicitada"].includes(String(r.status||"").toLowerCase())).length,scheduled:rows.filter(r=>["agendada","aprovada","aprovado","programado","agendado"].includes(String(r.status||"").toLowerCase())).length,active:rows.filter(r=>r.active_now).length,overdue:rows.filter(r=>r.overdue).length}),[rows]);
 const chartData=useMemo(()=>[{label:"Pend.",value:metrics.pending},{label:"Agend.",value:metrics.scheduled},{label:"Em gozo",value:metrics.active},{label:"Venc.",value:metrics.overdue}], [metrics]);
 const timeline=useMemo(()=>rows.slice().sort((a,b)=>String(a.period_start||a.start_date||"").localeCompare(String(b.period_start||b.start_date||""))).slice(-10),[rows]);
 return <section className="vacation-dashboard" aria-label="Painel de férias"><div className="vacation-kpis"><Kpi label="Pendentes" value={metrics.pending}/><Kpi label="Programadas" value={metrics.scheduled}/><Kpi label="Em férias hoje" value={metrics.active}/><Kpi label="Vencidas" value={metrics.overdue}/></div><div className="vacation-analytics"><div className="vacation-panel"><div className="vacation-panel-head"><div><h2>📊 Visão dos períodos</h2><p>Distribuição atual dos períodos de férias.</p></div></div><BarChart data={chartData}/></div><div className="vacation-panel vacation-summary"><div className="vacation-panel-head"><div><h2>Resumo</h2><p>Indicadores para tomada de decisão.</p></div></div><div className="summary-list"><Summary label="Programadas" value={metrics.scheduled}/><Summary label="Em férias hoje" value={metrics.active}/><Summary label="Pendentes" value={metrics.pending}/><Summary label="Vencidas" value={metrics.overdue}/></div></div></div><div className="vacation-panel"><div className="vacation-panel-head"><div><h2>{admin?"Central de Férias":"Férias da Minha Equipe"}</h2><p>{admin?"Acompanhamento geral de solicitações, programação e alertas.":"Acompanhe a programação de férias da estrutura sob sua gestão."}</p></div><span className="vacation-scope">{admin?"RH / DP":"GESTÃO"}</span></div><div className="vacation-table-wrap"><table><thead><tr><th>Colaborador</th><th>Início</th><th>Fim</th><th>Dias</th><th>Status</th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={r.id}><td><strong>{r.employee_name||r.colaborador||"Colaborador"}</strong></td><td>{formatDate(r.period_start||r.start_date)}</td><td>{formatDate(r.period_end||r.end_date)}</td><td>{r.days_taken??r.days??"—"}</td><td><span className="vacation-badge">{r.status||"Pendente"}</span></td></tr>):<tr><td colSpan="5" className="vacation-empty">Nenhum período encontrado.</td></tr>}</tbody></table></div></div><div className="vacation-timeline"><div className="vacation-panel-head"><div><h2>🕐 Linha do tempo</h2><p>Histórico e próximos períodos de férias em ordem cronológica.</p></div></div><div className="timeline-list">{timeline.length?timeline.map(r=><div className="timeline-item" key={`timeline-${r.id}`}><div className="timeline-dot"/><div className="timeline-content"><div className="timeline-date">{formatDate(r.period_start||r.start_date)} → {formatDate(r.period_end||r.end_date)}</div><strong>{r.employee_name||r.colaborador||"Colaborador"}</strong><span>{r.days_taken??r.days??0} dias · {r.status||"Pendente"}</span></div></div>):<p className="vacation-empty">Ainda não há eventos para exibir.</p>}</div></div><style>{`.vacation-dashboard{display:grid;gap:16px}.vacation-kpis{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}.vacation-kpi{border:1px solid var(--line);background:var(--panel);border-radius:var(--radius);padding:16px}.vacation-kpi-label{font-size:11px;color:var(--text-dim);text-transform:uppercase;font-weight:700}.vacation-kpi-value{font-size:26px;font-weight:800;margin-top:5px}.vacation-analytics{display:grid;grid-template-columns:minmax(0,1.5fr) minmax(280px,.7fr);gap:16px}.vacation-panel,.vacation-timeline{border:1px solid var(--line);border-radius:var(--radius);overflow:hidden;background:var(--panel)}.vacation-panel-head{padding:18px;display:flex;justify-content:space-between;gap:12px;align-items:flex-start;border-bottom:1px solid var(--line)}.vacation-panel-head h2{margin:0;font-size:17px}.vacation-panel-head p{margin:5px 0 0;color:var(--text-dim);font-size:13px}.vacation-scope{font-size:10px;font-weight:800;letter-spacing:.08em;color:var(--text-dim);padding:6px 9px;border:1px solid var(--line);border-radius:999px}.vacation-chart{padding:20px 22px 14px}.chart-bars{height:190px;display:flex;align-items:stretch;justify-content:space-around;gap:16px;border-bottom:1px solid var(--line);background:repeating-linear-gradient(to bottom,transparent 0,transparent 46px,var(--line) 47px)}.chart-col{height:100%;flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end;gap:8px}.chart-bar-wrap{height:160px;width:min(70px,70%);display:flex;align-items:flex-end;justify-content:center}.chart-bar{width:100%;min-height:6px;background:var(--amber);border-radius:7px 7px 0 0;display:flex;justify-content:center;align-items:flex-start;color:#fff;font-size:11px;font-weight:800;padding-top:5px;box-sizing:border-box;transition:height .2s ease}.chart-col small{font-size:10px;color:var(--text-dim);font-weight:700}.summary-list{padding:16px 18px;display:grid;gap:10px}.summary-row{display:flex;justify-content:space-between;align-items:center;padding:10px 0;border-bottom:1px solid var(--line)}.summary-row:last-child{border-bottom:0}.summary-row span{font-size:12px;color:var(--text-dim)}.summary-row strong{font-size:18px}.vacation-table-wrap{overflow-x:auto}.vacation-table-wrap table{width:100%;border-collapse:collapse}.vacation-table-wrap th,.vacation-table-wrap td{padding:11px 14px;text-align:left;border-bottom:1px solid var(--line);white-space:nowrap}.vacation-table-wrap th{font-size:10px;text-transform:uppercase;color:var(--text-dim)}.vacation-table-wrap td{font-size:13px}.vacation-badge{padding:4px 8px;border-radius:999px;border:1px solid var(--line);font-size:11px;font-weight:700}.vacation-empty{text-align:center!important;color:var(--text-dim);padding:20px}.timeline-list{padding:18px 22px 22px;position:relative}.timeline-list:before{content:"";position:absolute;left:33px;top:24px;bottom:24px;width:2px;background:var(--line)}.timeline-item{position:relative;display:flex;gap:18px;padding:0 0 18px}.timeline-dot{width:12px;height:12px;border-radius:50%;background:var(--amber);border:3px solid var(--panel);box-shadow:0 0 0 1px var(--line);z-index:1;margin:2px 0 0 5px;flex:none}.timeline-content{display:flex;flex-direction:column;gap:3px;font-size:13px}.timeline-date{font-size:11px;font-weight:800;color:var(--amber)}.timeline-content span{font-size:12px;color:var(--text-dim)}@media(max-width:900px){.vacation-kpis{grid-template-columns:repeat(2,1fr)}.vacation-analytics{grid-template-columns:1fr}.vacation-panel-head{flex-direction:column}}@media(max-width:600px){.vacation-kpis{grid-template-columns:1fr 1fr}.timeline-list{padding-left:16px}.timeline-list:before{left:26px}.timeline-dot{margin-left:0}}@media print{.vacation-kpis{grid-template-columns:repeat(4,1fr)}.vacation-analytics{grid-template-columns:1fr 1fr}.vacation-chart{break-inside:avoid}}`}</style></section>;
}
function Kpi({label,value}){return <div className="vacation-kpi"><div className="vacation-kpi-label">{label}</div><div className="vacation-kpi-value">{value}</div></div>}
function Summary({label,value}){return <div className="summary-row"><span>{label}</span><strong>{value}</strong></div>}
